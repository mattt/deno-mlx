/**
 * @deno-mlx/tensor — Layer 2: safe, idiomatic tensor wrapper (M2).
 *
 * Planned surface: a `Tensor` with `Symbol.dispose`/`using` lifetimes, async
 * `eval()`, a `tidy(fn)` scope for the token loop, typed shapes, and
 * method-chained ops. Scaffold only.
 */
export {};
