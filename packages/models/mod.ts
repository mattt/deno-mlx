/**
 * @deno-mlx/models — Layer 3: model runners (M3–M4).
 *
 * Planned surface: `generate()` (KV cache, sampling, streaming), Whisper
 * transcription, and embeddings. Tokenization via transformers.js; weights read
 * from the HF Hub cache as safetensors. Scaffold only.
 */
export {};
